# Codex Instructions For RESTOCK

When Steve asks to publish the integration, use the scripted HACS release flow:

```bash
python3 scripts/publish_integration.py VERSION "Short release note"
```

Keep README files short. They should mainly describe RESTOCK as a personal,
experimental project.
